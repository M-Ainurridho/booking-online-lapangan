export const apiUrl = (endpoint) => {
   return `https://api-booking-online-lapangan.vercel.app/${endpoint}`;
};

export const baseUrl = (endpoint) => {
   return `https://booking-online-lapangan.vercel.app/${endpoint}`;
};
